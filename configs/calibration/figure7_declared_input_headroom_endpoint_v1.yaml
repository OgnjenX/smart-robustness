schema_version: 1
name: figure7_declared_input_headroom_endpoint_v1
classification: source-bounded-behavioral-calibration-not-source-recovery
base_profile: configs/calibration/figure6_declared_interneuron_input_v1.yaml
learned_state:
  source_index: 40
  headroom_fraction: 1.0
protocol:
  duration_ms: 25.0
  dt_ms: 0.01
  top_down_current_pA: 800.0
  top_down_current_mode: until_cued_cell_first_event
  top_down_cue_lead_ms: 0.0
  uniform_relay_input_gain: 1.0
required_gates:
  - match_horizontal_relay_set
  - mismatch_overlap_only
  - match_more_active_relay_cells
  - match_more_trn_events
boundaries:
  - The learned-map shape and all unselected source rows remain unchanged.
  - The selected row is scaled only to the largest common factor allowed by archived adaptive maxima.
  - No comparator, input scaling, projection scaling, calcium ablation, or cue lead is permitted.
  - Passing this short mechanism screen only authorizes a fresh full-duration match.
